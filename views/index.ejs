<!DOCTYPE html>
<html lang="en">
  <head>
    <% include partials/_head.ejs %>

    <title>Resources</title>

    <script type="text/javascript" src="/scripts/app.js"></script>
    <script type="text/javascript" src="/scripts/carousel.js"> </script>
    <link rel="stylesheet" type="text/css" href="/styles/carousel.css" />
  </head>

  <body>
    <% include partials/_header.ejs %>

    <main class='container'>

      <article class='jumbotron'>
        <h1 class="text-left">Welcome to <span class="text-info">My Personal Syllabus</span>!<br/>
          <small class="text-muted">Your custom home for learning</small>
        </h1>
        <ul>
          <li><strong>Save any resource you find online that you'd like to learn</strong>&mdash;from tutorials, to blogs, to videos, and everything in between.</li>
          <li><strong>Never forget what you're interested in</strong> by saving to your own page.</li>
          <li><strong>Find what others are currently learning</strong> by searching through our extensive resource database.</li>
        </ul>
      </article>

      <article>
        <h1 id="title-for-carousel">Most recent resources</h1>
        <div id="carousel-container" class='carousel slide bg-light' data-ride="carousel">
          <ol class="carousel-indicators">
              <li id="carousel-indicators" data-target="#carousel-container" data-slide-to="0" class="active"></li>
              <li id="carousel-indicators" data-target="#carousel-container" data-slide-to="1"></li>
              <li id="carousel-indicators" data-target="#carousel-container" data-slide-to="2"></li>
            </ol>
          <div class="carousel-inner">
            <!-- First active resource (third oldest created) -->
            <div class="carousel-item active">
              <img id="carousel-image" class="d-block w-100" src="/resources/<%=resourceActive.id%>/screenshot">
              <div id="carousel-caption" class="carousel-caption d-non d-md-block">
                <h3 id="carousel-title" class="text-center"><%=resourceActive.title%></h3>
                <p id="carousel-description" class="text-center"><%=resourceActive.description%></p>
              </div>
            </div>
            <!-- Carousel two newest resources rendered here with script -->
          </div>

          <a class="carousel-control-prev" href="#carousel-container" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
          </a>
          <a class="carousel-control-next" href="#carousel-container" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>

        </div>
        <% if (!username) { %>
          <div id="index-register">
            <h2 class="text-success"> Register Now! </h2>
            <form action="/registration" method="GET" title="Click here to register" class="d-inline form-inline my-2 my-lg-0">
              <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Click HERE to register</button>
            </form>
            <p>Already registered? <a href="/login"><strong>Log in</strong></a></p>
          </div>
        <%  } %>
      </article>
    </main>
  </body>
</html>
